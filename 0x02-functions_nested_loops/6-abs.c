#include "main.h"

/**
 * _abs - fuction that computes the absolute value odf an integer
 * @n: data type to check
 * Return: 0
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
